AWSTemplateFormatVersion: '2010-09-09'
Description: XOC staging S3 storage for heavy snapshots.

Parameters:
  BucketName:
    Type: String
    Default: ''

Conditions:
  UseCustomBucketName: !Not [!Equals [!Ref BucketName, '']]

Resources:
  SnapshotsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !If
        - UseCustomBucketName
        - !Ref BucketName
        - !Sub 'xoc-staging-snapshots-${AWS::AccountId}'
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: AbortIncompleteMultipartUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7
  XocDocumentsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub 'xoc-staging-documents-${AWS::AccountId}'
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: AbortIncompleteMultipartUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7
  TxdxDocumentsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub 'txdx-staging-documents-${AWS::AccountId}'
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: AbortIncompleteMultipartUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7

Outputs:
  SnapshotsBucketName:
    Value: !Ref SnapshotsBucket
  SnapshotsBucketArn:
    Value: !GetAtt SnapshotsBucket.Arn
  XocDocumentsBucketName:
    Value: !Ref XocDocumentsBucket
  XocDocumentsBucketArn:
    Value: !GetAtt XocDocumentsBucket.Arn
  TxdxDocumentsBucketName:
    Value: !Ref TxdxDocumentsBucket
  TxdxDocumentsBucketArn:
    Value: !GetAtt TxdxDocumentsBucket.Arn
