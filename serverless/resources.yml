Resources:
  ApplicationEventBus:
    Type: AWS::Events::EventBus
    Properties:
      Name: ${self:service}-${self:custom.stage}-bus

Outputs:
  EventBusName:
    Value: ${self:service}-${self:custom.stage}-bus
